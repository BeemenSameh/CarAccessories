@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right",style="display:inline-flex"}))
    {
        @Html.AntiForgeryToken()
        @*<a href="#" class="header-wrapicon1 dis-block">
        <img src="~/Content/images/icons/icon-header-01.png" class="header-icon1" alt="ICON">
         </a>
         <span class="linedivide1"></span>
          *@
        <div class="dropdown">
            <a href="#" class="header-wrapicon1 dis-block dropdown-toggle" data-toggle="dropdown">
                <img src="~/Content/images/icons/icon-header-01.png" class="header-icon1" alt="ICON">
            </a>
            <div class="dropdown-menu">
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" ,@class="dropdown-item" })
                @Html.ActionLink("Edit Profile", "EditCustomerProfile", "Account", routeValues: null, htmlAttributes: new { title = "Manage", @class = "dropdown-item" })
                <a class="dropdown-item" href="javascript:document.getElementById('logoutForm').submit()">Log off</a>
            </div>
        </div>
       
    }
}
else
{
    @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })
    <span class="linedivide1"></span>
    @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
}
